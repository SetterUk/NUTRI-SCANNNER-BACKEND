services:
  - type: web
    name: nutri-scanner-api
    env: docker
    region: ohio
    plan: free
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GROQ_API_KEY
        sync: false
